# Preview
![image](preview.png)
# Setup
 the setup script should install ripgrep  
 so just clone and run it  
 ```bash
  git clone https://github.com/Sam-programs/nvim-setup.git &&
  cd nvim-setup &&
  ./setup.sh
```
